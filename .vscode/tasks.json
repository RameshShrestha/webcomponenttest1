{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run webcomponenttest",
      "type": "shell",
      "dependsOn": "Build webcomponenttest",
      "command": "if [ ! -f ${workspaceFolder}/node_modules/@sap/html5-repo-mock/index.js ] || [ ! -f ${workspaceFolder}/node_modules/@sap/approuter/approuter.js ]; then npm i @sap/approuter && npm i @sap/html5-repo-mock; fi",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    },
    {
      "label": "Build webcomponenttest",
      "type": "shell",
      "command": "if [ ! -d ${workspaceFolder}/myreactmodule/build ]; then npm run build-myreactmodule; fi",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}